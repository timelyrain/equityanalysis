{
  "buildCommand": "python3 -c \"import json,datetime; open('public/build.json','w').write(json.dumps({'built': datetime.datetime.now(datetime.timezone.utc).strftime('%d %b %Y, %H:%M UTC')}))\"",
  "functions": {
    "api/analyze.py": {
      "maxDuration": 120
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/analyze.py"
    },
    {
      "src": "/(.*)",
      "dest": "/public/$1"
    }
  ]
}
